name: fsharp

on:
  # Trigger the workflow every night at 4:17 AM UTC
  schedule:
    - cron: '17 4 * * *'

  # Allows to run this workflow manually from the Actions tab
  workflow_dispatch:

jobs:
  run-tests-on-local-machine:
    runs-on: self-hosted

    steps:
      # Check out repository's code onto local machine
      - name: Check out code
        uses: actions/checkout@v4

      # Testing command
      - name: Build images
        run: |
          echo "Building images fsharp"
          bash .github/workflows/rebuild_fsharp.sh
